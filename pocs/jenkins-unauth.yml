name: poc-yaml-jenkins-unauthorized
rules:
  - method: GET
    path: /manage
    follow_redirects: false
    expression: |
      status==200 && body.bcontains(b'<a href="configureTools"><img src=') && body.bcontains(b'<a href="log" class=" ">System Log</a>')
detail:
  author: timwhite
  links:
    - https://github.com/timwhitez/ 
